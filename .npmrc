# Faster installs with lockfile
package-lock=true

# Strict engine version
engine-strict=true

# Save exact versions
save-exact=true

# Production-only installs
production=false

# Allow legacy peer dependencies for Netlify/Vercel builds
legacy-peer-deps=true
